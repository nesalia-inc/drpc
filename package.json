{
  "name": "@deessejs/root",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "ci:publish": "pnpm publish -r --access public --no-git-checks"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0"
  }
}
